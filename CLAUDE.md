# Project Instructions

## Memory & Context
Always read `.claude/` folder at the start of each conversation:
- `.claude/memory.md` — your memory 
- `.claude/spec.md` — specification 

Do NOT use global auto memory (`~/.claude/projects/...`). All memory lives in `.claude/` inside the repo.
